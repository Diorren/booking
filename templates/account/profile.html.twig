{% extends 'base.html.twig' %}

{% block title('Modification du profile') %}

{% block body %}
	<div class="container">
		<h1>Modification du profil de ...</h1>

		{{ form_start(profile) }}
		{{ form_widget(profile) }}

		<button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('ads_list') }}" class="btn btn-secondary">Retour</a>
		{{ form_end(profile) }}

	</div>

{% endblock %}

