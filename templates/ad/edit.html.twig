{% extends 'base.html.twig' %}

{% block title("Modification d'une annonce") %}

{% form_theme form 'ad/_collections.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Modification d'une annonce</h1>
		{{ form_start(form) }}

		{{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
		{{ form_end(form) }}
	</div>
{% endblock %}

{% block _announce_images_widget %}
	<p>Vous pouvez ajouter des images complémentaires</p>
	<input type="hidden" id="widget-count" value="0">
	<button type="button" class="btn btn-primary" id="add-image">Ajouter une image</button>

	{{ form_widget(form) }}
{% endblock %}

{% block _announce_images_entry_row %}
	{{ form_widget(form) }}
{% endblock %}