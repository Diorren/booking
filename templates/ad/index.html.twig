{% extends 'base.html.twig' %}

{% block title("Liste d'annonces") %}

{% block body %}

<div class="container">
    <h1 class="mt-3 mb-3">{{ title }}</h1>
    <div class="row">
        {% for ad in ads %}

        {% include 'ad/_ad.html.twig' %}
        
        {% endfor %}
    </div>    
</div>
{% endblock %}
